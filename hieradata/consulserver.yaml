---
classes:
  - role::consulserver

consul::version: '0.6.4'
consul::bin_dir: '/usr/bin'
consul::config_hash:
  bootstrap_expect: "1"
  data_dir: "/opt/consul"
  datacenter: "east-aws"
  log_level: "INFO"
  node_name: "consul-server"
  server: true
  ui_dir: "/opt/consul/ui"
