---
classes:
  - role::galera

consul::config_hash:
  data_dir: "/opt/consul"
  datacenter: "east-aws"
  log_level: "INFO"
  node_name: "%{::hostname}"
  bind_addr: "%{::ipaddress_eth0}"
  server: "false"
  start_join: "%{hiera('join_addr')}"
