---
:backends:
  - eyaml
  - yaml
:eyaml:
  :datadir: /etc/puppet/hieradata
:yaml:
  :datadir: /etc/puppet/hieradata
:hierarchy:
  - "%{::init_env}/%{::init_role}"
  - "%{::init_role}"
  - "%{::init_env}"
  - common
  - mysql
